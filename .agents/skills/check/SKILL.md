---
name: check
description: Run the full check (lint + typecheck + test with coverage) and fix failures until green.
---

Run `pnpm check` (lint + typecheck + test). If anything fails, fix it and re-run until green. Report what failed and what you changed.
