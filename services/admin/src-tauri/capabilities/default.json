{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window with no plugin or filesystem permissions",
  "windows": ["main"],
  "permissions": ["allow-api-request", "allow-clear-session"]
}
